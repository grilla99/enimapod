{
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.218.0",
    "@aws-sdk/client-secrets-manager": "^3.218.0",
    "aws-sdk": "^2.1263.0",
    "axios": "^1.1.3"
  },
  "name": "web",
  "description": "Directory to store web files and nginx server files",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
